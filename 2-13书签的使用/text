书签的使用
地址 https://www.vim.org/scripts/download_script.php?src_id=1026
将下载好的文件visualMark.vim拷贝到~/.vim/plugin

配置：
	在该文件的第55行代码
	将exec ":lan mes en US"改为exec ":lan POSIX"
使用：（只在当前文件有效，关闭文件消失）
	在普通模式下，使用mm将当前光标行加入标签
	使用F2键在标签来回转跳
	再次在标签处使用mm可以删除标记
	
