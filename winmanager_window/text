		安装和使用
解压：
	unzip winmanager.zip
拷贝：
	cp doc/winmanager.txt ~/.vim/doc/
	cp plugin/*.vim ~/.vim/plugin/
	其中doc和plugin目录是解压winmanager.zip得到
使用：
	:WMToggle列出当前目录的文件，双击打开
	:WMClose 关闭上一个命令打开的窗口
配置：
	:nmap <A-w> :WMToggle<CR> 使用快件键打开Alt+w
	let g:winManagerWindowLayout='FileExplorer|TagList' 将文件列表和函数宏定义列表在不同窗口显示
	:nmap <A-q> :FirstExplorerWindow<CR> 跳转到屏幕的左上角窗口	
	:nmap <A-z> :BottomExplorerWindow<CR> 跳转到屏幕的左下角窗口	
	let g:winManagerWindowLayout='FileExplorer,TagList' 将文件列表和函数宏定义列表在同窗口显示
	在同一个窗口显示时，使用Ctrl+n切换窗口



